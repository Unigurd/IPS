fun [char] main() =
    let a = write(2 * 3) in                  // 6
    let b = write(3 / 2) in                  // 1
    let c = write(2 + 3 * 5 + 6) in          // 23
    let d = write(10 / 2 - 5 / 3) in         // 4
    let e = write((10 + 3) * 3) in           // 39
    let f = write(10 + (3 * 3)) in           // 19
    let g = write(~ 10 + 3) in
    let g = write(~ 20 - 3) in
    "hej"